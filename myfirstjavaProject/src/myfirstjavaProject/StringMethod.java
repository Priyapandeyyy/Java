package myfirstjavaProject;

public class StringMethod {
	
	public static void main(String[] args) {
		
		/* String = a reference data type that can store one or more characters
		           reference data types have access to useful methods.
		           */
		
		String name = "Priya";
		
//		boolean result = name.equals("Priya");       (Compare two strings to check if they are equal.)
//		boolean result = name.equalsIgnoreCase("Priya");      (will ignore the equal) 
//		int result = name.length();    (will tell the length of the string)
//		char result = name.charAt(3);     (will tell at which number which value is)
//		int result = name.indexOf("y");   (will tell the number opposite of charAt).
//		boolean result = name.isEmpty();  (checks whether the string is empty or not if yes ten true if not then false).
//		String result = name.toUpperCase();   (converts the string to uppercase)
//		String result = name.toLowerCase();   (converts the string to lowercase)
//		String result = name.trim();   (will remove all the empty spaces)
//		String result = name.replace("a", "o");  (will replace)
				
		System.out.println(result);
	}
}
